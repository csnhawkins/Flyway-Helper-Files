If "flyway-capture-schema" and "flyway-generate-migration" skills are available in
this project, use them for the workflow steps and safety rules - run capture
first, then generation, since they are separate skills for separate stages of the
same job. If they aren't available, follow this fallback:
  Check the development database for schema changes not yet in the schema model.
  For each change, get full diff details (not just which object changed, but what
  changed about it) before updating the schema model. Then generate a migration
  script and run a code review on it.

SCOPE: run the full workflow end to end - both capturing changes into the schema
model AND generating and reviewing a migration script for them. Don't stop partway.

MANDATORY: you must actually call the Flyway MCP tools (load_project, then a diff
tool, etc.) to reach any conclusion - never respond with "no_changes" or any other
status based on assumption, prior knowledge, or without tool calls to back it up.
Answering without having called at least load_project and a diff tool is treated as
a failed run, not a valid "no changes" result.

This is an unattended pipeline run - there is no one available to answer a question.
Never pause to ask for confirmation, even for a destructive or risky change (e.g. a
dropped column) - do not use AskUserQuestion or any other tool that pauses for human
input, it is not available in this run and attempting it wastes a turn. Instead,
proceed, and record it as a high-severity issue in your response so a human can
review and approve or reject it afterwards.

Do not use git, bash, or any tool other than the Flyway MCP tools available to you.
The pipeline handles committing, branching, and opening a pull request separately -
your job is only to update the schema model, generate the script, and review it.

Regardless of whether a skill was available, when you are finished respond with
ONLY a single, raw JSON object and no other text - this applies even if there was
nothing to do, even if a tool call was denied or failed, and even if you are only
partway through the workflow when you run out of turns. Do not wrap the JSON in
markdown code fences or backticks (no ```json). Do not explain yourself in plain
prose under any circumstances - if something went wrong, put that explanation in the
"issues" field of the JSON below, not in free text outside it. Do not use emoji
anywhere in your response. Use exactly these field names - do not rename, omit, or
add fields:
{
  "status": "clean" | "issues_found" | "no_changes",
  "migrationFile": "<path to the generated migration script, relative to the project folder, or null>",
  "description": "<short one-line description of the schema change, or null>",
  "changes": [{"type": "Add|Edit|Drop", "object": "<schema.object>", "objectType": "<Table|View|Procedure|...>", "details": "<specific, e.g. 'Dropped column ReviewText (nvarchar(1000))' or 'Added column Foo (int, not null)' - not just 'edited'>"}],
  "reviewSummary": "<one paragraph plain-English summary of the code review result, or a one-line note that there was nothing to change>",
  "rulesChecked": <number of rules checked, or 0>,
  "issues": ["<issue description - include anything you'd normally have asked for confirmation on, marked High severity>", "..."]
}

If there are no changes to the development database, respond with exactly this (still no
markdown fence, still these exact field names):
{"status": "no_changes", "migrationFile": null, "description": null, "changes": [], "reviewSummary": "No schema changes were found in the development database.", "rulesChecked": 0, "issues": []}
