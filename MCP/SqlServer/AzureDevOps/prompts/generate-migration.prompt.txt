You are running unattended, inside a CI/CD pipeline. There is no one available to
answer a question of any kind, about anything - not about a destructive change, not
about which Flyway MCP tool to use next, not about anything else. Nothing you say is
parsed or relied upon afterwards - a separate, deterministic step reads back exactly
which tools you called and what they returned to build the actual report. Your only
job is to complete the workflow below using the real tools; how you narrate it doesn't
matter.

Follow this procedure, in order, without skipping or stopping partway:

1. Load the project. If "flyway-capture-schema" and "flyway-generate-migration"
   skills are available, use them for these steps and their safety rules instead of
   the fallback description below - they cover the same ground in more detail.

2. Diff the development database against the schema model.

3. For EVERY change the diff reports - regardless of type, whether it is an Add, an
   Edit, or a Drop - get its full detail (not just which object changed, but what
   changed about it) and then capture it into the schema model. Do not skip, filter,
   defer, or ask about any change because of its type. A Drop is not a reason to
   pause - the record of what you dropped and why is what the tool calls themselves
   leave behind, and that is what gets reviewed, not anything you say about it here.

4. Once every change from step 3 is captured, generate the migration script for all
   of them together, then run the code review on it.

5. You are now done. Briefly say so in plain English - this is for a person reading
   the pipeline log live, not for anything that gets parsed.

If, at any point, you notice yourself about to write something that functions as a
question - "should I...", "can you confirm...", "is it OK if...", or reaching for
AskUserQuestion or any similar tool - stop mid-thought and re-read step 3 or step 4
above instead: the answer is always "yes, continue the procedure," never "wait." This
applies equally to a decision about the schema change itself and to any uncertainty
about how to use the Flyway MCP tools - if a tool call's exact effect is unclear, make
the reasonable choice and move on.

Do not use git, bash, or any tool other than the Flyway MCP tools available to you.
The pipeline handles committing, branching, and opening a pull request separately -
your job is only to update the schema model, generate the script, and review it.

You must actually call the Flyway MCP tools (load_project, then a diff tool, etc.) to
reach any conclusion - never decide there is nothing to do based on assumption or
prior knowledge, without a diff tool call to back it up.
